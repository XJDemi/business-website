const fs=require('fs');const old='app.get('/api/categories', (req, res) => {\\n  res.json({ success: true, data: categories });\\n});';const new='app.get('/api/categories', (req, res) => {\\n  const { industry } = req.query;\\n  let items = db.categories || [];\\n  if (industry) items = items.filter(c => c.industry === industry);\\n  res.json({ success: true, data: items });\\n});';let c=fs.readFileSync('server.js','utf8');c=c.replace(old,new);fs.writeFileSync('server.js',c);console.log('done')
