# Vercel Environment Variables Setup Guide
# 
# 在Vercel项目设置中配置以下环境变量:
# 
# JWT_SECRET - JWT密钥（自定义一个安全的字符串）
#   例如: xuanji-secret-key-2024-production
#
# SUPABASE_URL - Supabase项目URL
#   可在Supabase Dashboard > Project Settings > API中找到
#   格式: https://xxxxxxx.supabase.co
#
# SUPABASE_ANON_KEY - Supabase匿名密钥
#   可在Supabase Dashboard > Project Settings > API中找到
#   注意：不要使用SERVICE_ROLE_KEY
#
# SMTP_USER - Gmail SMTP用户名（用于发送询盘通知）
# SMTP_PASS - Gmail SMTP密码（应用专用密码）
#
# 可选配置:
# STORAGE_PROVIDER - 存储提供者（默认supabase）
# STORAGE_BUCKET - 存储桶名称（默认product-images）

# 复制此文件为.env并填入实际值进行本地开发
JWT_SECRET=your-jwt-secret-here
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key-here

# SMTP配置（可选）
SMTP_USER=
SMTP_PASS=

# 存储配置
STORAGE_PROVIDER=supabase
STORAGE_BUCKET=product-images
