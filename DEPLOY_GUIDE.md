# 网站部署详细指南（小白专用版）

---

## ❗ 这是最简单的方案！

**使用宝塔面板** — 像操作 Windows 一样管理服务器，完全不需要命令行！

**成本**：阿里云轻量应用服务器 ￥34/月（香港）

**效果**：和您本地 `http://localhost:8000` 一模一样

---

## 📋 第一步：购买服务器

1. 打开浏览器，访问：https://www.aliyun.com/
2. 注册账号（用手机号），登录
3. 搜索 **"轻量应用服务器"**，点击进入
4. 点击 **"立即购买"**

5. 配置服务器（照着选就行）：
   - **地域**：选择 **香港**（国外客户访问更快）
   - **镜像**：选择 **宝塔面板 7.x**（这个最重要！）
   - **套餐**：选最便宜的（2核2G，￥34/月）
   - **购买时长**：选1年（更便宜）
   - **设置密码**：设置一个安全密码（一定要记住！）

6. 点击 **"立即购买"**，付款

7. 等待 1-2 分钟，服务器创建完成

---

## 🖥️ 第二步：登录宝塔面板

1. 在阿里云控制台，找到您的服务器
2. 点击服务器名称，进入详情页
3. 找到 **"宝塔面板信息"**，记下：
   - **面板地址**：类似 `http://47.123.45.67:8888/abc123`
   - **面板账号**：`username`
   - **面板密码**：`password`

4. 打开浏览器，输入面板地址，按回车
5. 输入账号密码，点击 **"登录"**

6. 登录成功后，会看到一个漂亮的管理界面！🎉

---

## 📦 第三步：安装 Node.js

1. 在宝塔面板左侧，点击 **"软件商店"**
2. 在搜索框输入 **"PM2管理器"**
3. 点击 **"安装"**

4. 等待安装完成后，点击 **"设置"**
5. 在弹出的窗口中，点击 **"版本管理"**
6. 选择 **"Node.js 20.x"**，点击 **"安装"**

7. 等待安装完成（约1分钟）

---

## 📁 第四步：上传网站文件

### 方法一：使用宝塔文件管理器（最简单）

1. 在宝塔面板左侧，点击 **"文件"**
2. 进入 `/www/wwwroot` 目录
3. 点击 **"上传"** → **"上传文件"**

4. 回到您的电脑，找到网站文件夹：`e:\郭海娥\trae 项目\business-website`
5. **注意：不要上传 `node_modules` 文件夹！**
6. 选中除了 `node_modules` 以外的所有文件和文件夹
7. 拖到宝塔的上传区域

8. 等待上传完成（文件较多，可能需要几分钟）

### 方法二：使用 FTP 工具（更稳定）

1. 在宝塔面板左侧，点击 **"软件商店"**
2. 搜索 **"Pure-FTPd"**，点击 **"安装"**

3. 安装完成后，点击 **"设置"** → **"添加FTP账号"**
4. 填写：
   - **用户名**：`xuanji`
   - **密码**：设置一个密码
   - **目录**：选择 `/www/wwwroot`
   - 点击 **"提交"**

5. 下载 FTP 工具：https://filezilla-project.org/
6. 安装并打开 FileZilla
7. 填写：
   - **主机**：您的服务器IP
   - **用户名**：`xuanji`
   - **密码**：您设置的密码
   - **端口**：21
8. 点击 **"快速连接"**

9. 连接成功后，左边是您的电脑，右边是服务器
10. 在左边找到网站文件夹 `e:\郭海娥\trae 项目\business-website`
11. **不要上传 `node_modules` 文件夹！**
12. 把其他所有文件拖到右边的 `/www/wwwroot` 目录

---

## 🚀 第五步：启动网站

1. 在宝塔面板左侧，点击 **"软件商店"**
2. 找到 **"PM2管理器"**，点击 **"设置"**

3. 在 PM2 设置页面，点击 **"添加项目"**
4. 填写：
   - **项目名称**：`xuanji`
   - **启动文件**：`/www/wwwroot/server.js`
   - **运行目录**：`/www/wwwroot`
5. 点击 **"提交"**

6. 等待几秒钟，网站就启动了！
7. 您可以在 PM2 管理器中看到网站状态是 **"运行中"**

---

## 🌐 第六步：配置域名

### 6.1 配置宝塔网站

1. 在宝塔面板左侧，点击 **"网站"**
2. 点击 **"添加站点"**
3. 填写：
   - **域名**：输入您的域名，如 `www.yourdomain.com`
   - **根目录**：`/www/wwwroot`
   - **PHP版本**：选择 **"纯静态"**
4. 点击 **"提交"**

### 6.2 配置域名解析

1. 登录您购买域名的网站（阿里云、腾讯云等）
2. 找到 **"域名管理"** → **"DNS解析"**
3. 添加两条记录：

   | 记录类型 | 主机记录 | 记录值 |
   |---------|---------|-------|
   | A       | @       | 您的服务器IP |
   | A       | www     | 您的服务器IP |

4. 保存配置，等待 5-30 分钟生效

### 6.3 配置反向代理

1. 在宝塔面板的网站列表中，找到您的网站，点击 **"设置"**
2. 在左侧菜单点击 **"反向代理"**
3. 点击 **"添加反向代理"**
4. 填写：
   - **代理名称**：`xuanji`
   - **目标URL**：`http://127.0.0.1:8000`
5. 点击 **"提交"**

---

## 🔒 第七步：配置 HTTPS（免费证书）

1. 在宝塔面板的网站设置中，点击 **"SSL"**
2. 选择 **"Let's Encrypt"**
3. 勾选您的域名（`yourdomain.com` 和 `www.yourdomain.com`）
4. 点击 **"申请"**

5. 等待约 1 分钟，证书自动安装完成
6. 开启 **"强制HTTPS"**

---

## ✅ 第八步：验证网站

1. 打开浏览器，访问：`https://www.yourdomain.com`
2. 您应该能看到和本地一模一样的网站！
3. 测试后台管理：`https://www.yourdomain.com/admin`
4. 默认账号：`admin` / `admin123`

---

## 🔐 第九步：修改默认密码（非常重要！）

1. 登录后台管理：`https://www.yourdomain.com/admin`
2. 点击右上角的 **"设置"** 或个人头像
3. 找到修改密码的选项
4. 设置一个新密码（不要用 `admin123`！）

---

## ❓ 常见问题

### Q1: 网站打不开？
**A**: 请检查：
- 服务器是否正常运行（阿里云控制台查看状态）
- PM2管理器中网站是否显示"运行中"
- 域名解析是否生效（等待30分钟）

### Q2: 后台登录不了？
**A**: 默认账号是 `admin` / `admin123`，请确认密码正确

### Q3: 上传产品图片失败？
**A**: 请检查权限：
1. 在宝塔面板点击 **"文件"**
2. 找到 `/www/wwwroot/uploads` 文件夹
3. 右键点击 → **"权限"**
4. 勾选所有权限，点击 **"确定"**

### Q4: 想更新网站内容？
**A**: 
1. 在本地修改代码
2. 使用宝塔文件管理器或FTP上传覆盖
3. 在PM2管理器中，点击网站右侧的 **"重启"**

---

## 📞 需要帮助？

如果遇到任何问题，请告诉我：
1. 您卡在了哪一步
2. 截图给我看错误信息
3. 我会帮您一步步解决！

---

## 🎉 恭喜！

您的网站已经成功部署到互联网上了！

- 主站地址：`https://www.yourdomain.com`
- 后台管理：`https://www.yourdomain.com/admin`